name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-macos-arm64:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build -c release
      - name: Package
        run: |
          cp .build/release/orbital orbital-darwin-arm64
          chmod +x orbital-darwin-arm64
      - uses: actions/upload-artifact@v4
        with:
          name: orbital-darwin-arm64
          path: orbital-darwin-arm64

  build-macos-x86_64:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build -c release
      - name: Package
        run: |
          cp .build/release/orbital orbital-darwin-x86_64
          chmod +x orbital-darwin-x86_64
      - uses: actions/upload-artifact@v4
        with:
          name: orbital-darwin-x86_64
          path: orbital-darwin-x86_64

  build-linux-x86_64:
    runs-on: ubuntu-latest
    container: swift:6.0
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build -c release
      - name: Package
        run: |
          cp .build/release/orbital orbital-linux-x86_64
          chmod +x orbital-linux-x86_64
      - uses: actions/upload-artifact@v4
        with:
          name: orbital-linux-x86_64
          path: orbital-linux-x86_64

  build-linux-arm64:
    runs-on: ubuntu-24.04-arm
    container: swift:6.0
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build -c release
      - name: Package
        run: |
          cp .build/release/orbital orbital-linux-arm64
          chmod +x orbital-linux-arm64
      - uses: actions/upload-artifact@v4
        with:
          name: orbital-linux-arm64
          path: orbital-linux-arm64

  release:
    needs: [build-macos-arm64, build-macos-x86_64, build-linux-x86_64, build-linux-arm64]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          gh release create "$tag" \
            --repo "$GITHUB_REPOSITORY" \
            --title "$tag" \
            --generate-notes \
            artifacts/orbital-darwin-arm64/orbital-darwin-arm64 \
            artifacts/orbital-darwin-x86_64/orbital-darwin-x86_64 \
            artifacts/orbital-linux-x86_64/orbital-linux-x86_64 \
            artifacts/orbital-linux-arm64/orbital-linux-arm64
